<UserControl x:Class="Stock.UI.Views.ContentGridPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stock.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <DataGrid AlternatingRowBackground="Gray" ItemsSource="{Binding DataVenderTable}"
              Margin='8'
              AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id, Mode=TwoWay}" />
            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name, Mode=TwoWay}" />
            <DataGridTextColumn Header="Address" Binding="{Binding Path=Address, Mode=TwoWay}" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
